JAD KRONO — Vercel npm installation repair

This build fixes the dependency installation failure by:
1. Removing private/internal registry URLs from package-lock.json.
2. Pinning the Vercel Node.js runtime to Node 22.x (npm 10).
3. Using a clean temporary npm cache and the public npm registry during deployment.

For an existing GitHub Desktop checkout, replace these three root files:
- package.json
- package-lock.json
- vercel.json

Commit, push, then redeploy without the existing Build Cache.
